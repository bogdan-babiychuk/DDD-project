# Kafka Chat Frontend

Современный React фронтенд для чат-приложения на FastAPI и Kafka.

## Возможности

- 🚀 Создание новых чатов
- 💬 Отправка и получение сообщений в реальном времени
- 🎨 Современный и отзывчивый UI с Tailwind CSS
- 📱 Адаптивный дизайн для всех устройств
- 🔄 Автоматическое обновление сообщений
- ⚡ TypeScript для типобезопасности

## Технологии

- **React 18** - современная библиотека для создания пользовательских интерфейсов
- **TypeScript** - типизированный JavaScript
- **Tailwind CSS** - utility-first CSS фреймворк
- **Axios** - HTTP клиент для API запросов
- **date-fns** - библиотека для работы с датами

## Установка

1. Убедитесь, что у вас установлен Node.js версии 16 или выше
2. Перейдите в папку frontend:
   ```bash
   cd frontend
   ```

3. Установите зависимости:
   ```bash
   npm install
   ```

## Запуск

### Разработка

Для запуска в режиме разработки:

```bash
npm start
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

### Сборка для продакшена

Для создания оптимизированной сборки:

```bash
npm run build
```

Собранные файлы будут помещены в папку `build/`

## Конфигурация

### API URL

По умолчанию фронтенд настроен на работу с API по адресу `http://localhost:8000`.

Для изменения URL API создайте файл `.env` в корне папки `frontend`:

```env
REACT_APP_API_URL=http://your-api-url.com
```

## Структура проекта

```
src/
├── components/          # React компоненты
│   ├── CreateChat.tsx  # Создание нового чата
│   ├── ChatList.tsx    # Список чатов
│   ├── ChatView.tsx    # Просмотр чата
│   ├── MessageList.tsx # Список сообщений
│   └── MessageInput.tsx # Ввод сообщений
├── api/                # API клиент
│   └── client.ts       # Axios клиент и методы API
├── types/              # TypeScript типы
│   └── api.ts          # Типы для API
├── App.tsx             # Главный компонент
└── index.tsx           # Точка входа
```

## API Endpoints

Фронтенд использует следующие эндпоинты вашего FastAPI:

- `POST /chat/` - создание нового чата
- `GET /chat/{chat_id}/` - получение информации о чате
- `POST /chat/{chat_id}/messages` - отправка сообщения
- `GET /chat/{chat_id}/messages` - получение сообщений чата

## Разработка

### Добавление новых компонентов

1. Создайте новый файл в папке `src/components/`
2. Используйте TypeScript для типизации пропсов
3. Применяйте Tailwind CSS классы для стилизации
4. Добавьте компонент в `App.tsx` или другой родительский компонент

### Работа с API

Все API вызовы должны проходить через `src/api/client.ts`. Добавьте новые методы в объект `chatApi`.

### Стилизация

Используйте Tailwind CSS классы для стилизации. Для кастомных стилей добавьте их в `src/index.css`.

## Скриншоты

Приложение имеет современный дизайн с:
- Адаптивной сеткой
- Карточным дизайном
- Плавными анимациями
- Интуитивным интерфейсом

## Поддержка

При возникновении проблем:
1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что ваш FastAPI сервер запущен
3. Проверьте настройки CORS на бэкенде

## Лицензия

MIT
